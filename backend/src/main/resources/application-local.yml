# 로컬 개발용 설정 (.env 파일과 함께 사용)
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update   # 로컬에서는 자동 스키마 업데이트 허용

logging:
  level:
    com.sancheck: DEBUG
    org.springframework.security: DEBUG
